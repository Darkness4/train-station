get:
  tags:
    - other
  summary: Root
  description: Root
  operationId: getRoot
  responses:
    302:
      description: Redirect to API
      headers:
        Location:
          description: URI to the API
          schema:
            type: string
